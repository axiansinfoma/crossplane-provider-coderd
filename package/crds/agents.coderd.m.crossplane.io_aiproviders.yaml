---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: aiproviders.agents.coderd.m.crossplane.io
spec:
  group: agents.coderd.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - coderd
    kind: AIProvider
    listKind: AIProviderList
    plural: aiproviders
    singular: aiprovider
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AIProvider is the Schema for the AIProviders API. ~> This resource
          is experimental. Changes are expected, and it is not recommended for production
          use. -> _wo attributes are write-only https://developer.hashicorp.11 or
          later. Configures an AI Provider for use with Coder's AI Gateway & Coder
          Agents. For type = "bedrock", omit settings.bedrock.access_key_wo and settings.bedrock.access_key_secret_wo
          to use the AWS SDK default credential chain as resolved by the Coder server
          process (IAM role, IRSA, environment variables, shared config, SSO, IMDS,
          and more). Set both together to use static IAM-user credentials.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AIProviderSpec defines the desired state of AIProvider
            properties:
              forProvider:
                properties:
                  apiKeyWoSecretRef:
                    description: |-
                      only) Plaintext API key for the provider. Not valid for bedrock or copilot, or when settings.bedrock is set. Bump api_key_wo_version to rotate it.
                      Plaintext API key for the provider. Not valid for `bedrock` or `copilot`, or when `settings.bedrock` is set. Bump `api_key_wo_version` to rotate it.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  apiKeyWoVersion:
                    description: |-
                      only API key. Required when api_key_wo is set; bump it whenever api_key_wo changes to rotate the stored key.
                      Version for the write-only API key. Required when `api_key_wo` is set; bump it whenever `api_key_wo` changes to rotate the stored key.
                    type: number
                  baseUrl:
                    description: |-
                      (String) Absolute HTTP(S) base URL for the upstream provider endpoint.
                      Absolute HTTP(S) base URL for the upstream provider endpoint.
                    type: string
                  displayName:
                    description: |-
                      (String) Display name shown in Coder. If omitted, defaults to the provider name.
                      Display name shown in Coder. If omitted, defaults to the provider name.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether this AI provider is enabled. Defaults to true.
                      Whether this AI provider is enabled. Defaults to true.
                    type: boolean
                  name:
                    description: |-
                      (String) Unique provider name. Must be lowercase alphanumeric words separated by hyphens.
                      Unique provider name. Must be lowercase alphanumeric words separated by hyphens.
                    type: string
                  settings:
                    description: specific provider settings. (see below for nested
                      schema)
                    properties:
                      bedrock:
                        description: (Attributes) AWS Bedrock settings. Valid only
                          for type = "bedrock" or type = "anthropic". (see below for
                          nested schema)
                        properties:
                          accessKeySecretWoSecretRef:
                            description: |-
                              only) AWS secret access key for Bedrock.
                              AWS secret access key for Bedrock.
                            properties:
                              key:
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                            required:
                            - key
                            - name
                            type: object
                          accessKeyWoSecretRef:
                            description: |-
                              only) AWS access key ID for Bedrock. See Coder's Amazon Bedrock provider docs.
                              AWS access key ID for Bedrock. See [Coder's Amazon Bedrock provider docs](https://coder.com/docs/ai-coder/ai-gateway/providers#amazon-bedrock).
                            properties:
                              key:
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                            required:
                            - key
                            - name
                            type: object
                          credentialsWoVersion:
                            description: |-
                              only credentials. Bump this value to send, rotate, or clear credentials.
                              Version for Bedrock write-only credentials. Bump this value to send, rotate, or clear credentials.
                            type: number
                          model:
                            description: |-
                              (String) Primary Bedrock model identifier.
                              Primary Bedrock model identifier.
                            type: string
                          protocol:
                            description: |-
                              model" (default) translates the native Messages request into Bedrock's InvokeModel format and requires model and small_fast_model. "mantle" forwards the native Messages request body unchanged with AWS SigV4 signing and passes through the client's model; requires region (the Mantle base URL is not a canonical Bedrock URL, so the region is never derived from base_url). Requires Coder v2.36.0 or later for the Mantle protocol. Omit (or set to "invoke-model") to use the default. Removing the attribute clears it back to the default; Coder maps an empty value to "invoke-model".
                              Bedrock wire protocol. `"invoke-model"` (default) translates the native Messages request into Bedrock's InvokeModel format and requires `model` and `small_fast_model`. `"mantle"` forwards the native Messages request body unchanged with AWS SigV4 signing and passes through the client's model; requires `region` (the Mantle base URL is not a canonical Bedrock URL, so the region is never derived from `base_url`). Requires Coder v2.36.0 or later for the Mantle protocol. Omit (or set to `"invoke-model"`) to use the default. Removing the attribute clears it back to the default; Coder maps an empty value to `"invoke-model"`.
                            type: string
                          region:
                            description: |-
                              (String) AWS region for Bedrock. If omitted, derived from the canonical Bedrock base_url attribute.
                              AWS region for Bedrock. If omitted, derived from the canonical Bedrock `base_url` attribute.
                            type: string
                          roleArn:
                            description: |-
                              (String) ARN of an AWS IAM role to assume via STS before calling Bedrock. The base identity (the AWS SDK default credential chain or the static credentials) signs the AssumeRole call, and the temporary credentials sign Bedrock requests. Omit to call Bedrock with the base identity directly. Requires Coder v2.35.0 or later.
                              ARN of an AWS IAM role to assume via STS before calling Bedrock. The base identity (the AWS SDK default credential chain or the static credentials) signs the AssumeRole call, and the temporary credentials sign Bedrock requests. Omit to call Bedrock with the base identity directly. Requires Coder v2.35.0 or later.
                            type: string
                          smallFastModel:
                            description: |-
                              (String) Small/fast Bedrock model identifier used for background tasks.
                              Small/fast Bedrock model identifier used for background tasks.
                            type: string
                        type: object
                    type: object
                  type:
                    description: |-
                      compat, openrouter, vercel, and copilot.
                      AI provider type. Valid values are `openai`, `anthropic`, `azure`, `bedrock`, `google`, `openai-compat`, `openrouter`, `vercel`, and `copilot`.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  apiKeyWoSecretRef:
                    description: |-
                      only) Plaintext API key for the provider. Not valid for bedrock or copilot, or when settings.bedrock is set. Bump api_key_wo_version to rotate it.
                      Plaintext API key for the provider. Not valid for `bedrock` or `copilot`, or when `settings.bedrock` is set. Bump `api_key_wo_version` to rotate it.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  apiKeyWoVersion:
                    description: |-
                      only API key. Required when api_key_wo is set; bump it whenever api_key_wo changes to rotate the stored key.
                      Version for the write-only API key. Required when `api_key_wo` is set; bump it whenever `api_key_wo` changes to rotate the stored key.
                    type: number
                  baseUrl:
                    description: |-
                      (String) Absolute HTTP(S) base URL for the upstream provider endpoint.
                      Absolute HTTP(S) base URL for the upstream provider endpoint.
                    type: string
                  displayName:
                    description: |-
                      (String) Display name shown in Coder. If omitted, defaults to the provider name.
                      Display name shown in Coder. If omitted, defaults to the provider name.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether this AI provider is enabled. Defaults to true.
                      Whether this AI provider is enabled. Defaults to true.
                    type: boolean
                  name:
                    description: |-
                      (String) Unique provider name. Must be lowercase alphanumeric words separated by hyphens.
                      Unique provider name. Must be lowercase alphanumeric words separated by hyphens.
                    type: string
                  settings:
                    description: specific provider settings. (see below for nested
                      schema)
                    properties:
                      bedrock:
                        description: (Attributes) AWS Bedrock settings. Valid only
                          for type = "bedrock" or type = "anthropic". (see below for
                          nested schema)
                        properties:
                          accessKeySecretWoSecretRef:
                            description: |-
                              only) AWS secret access key for Bedrock.
                              AWS secret access key for Bedrock.
                            properties:
                              key:
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                            required:
                            - key
                            - name
                            type: object
                          accessKeyWoSecretRef:
                            description: |-
                              only) AWS access key ID for Bedrock. See Coder's Amazon Bedrock provider docs.
                              AWS access key ID for Bedrock. See [Coder's Amazon Bedrock provider docs](https://coder.com/docs/ai-coder/ai-gateway/providers#amazon-bedrock).
                            properties:
                              key:
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                            required:
                            - key
                            - name
                            type: object
                          credentialsWoVersion:
                            description: |-
                              only credentials. Bump this value to send, rotate, or clear credentials.
                              Version for Bedrock write-only credentials. Bump this value to send, rotate, or clear credentials.
                            type: number
                          model:
                            description: |-
                              (String) Primary Bedrock model identifier.
                              Primary Bedrock model identifier.
                            type: string
                          protocol:
                            description: |-
                              model" (default) translates the native Messages request into Bedrock's InvokeModel format and requires model and small_fast_model. "mantle" forwards the native Messages request body unchanged with AWS SigV4 signing and passes through the client's model; requires region (the Mantle base URL is not a canonical Bedrock URL, so the region is never derived from base_url). Requires Coder v2.36.0 or later for the Mantle protocol. Omit (or set to "invoke-model") to use the default. Removing the attribute clears it back to the default; Coder maps an empty value to "invoke-model".
                              Bedrock wire protocol. `"invoke-model"` (default) translates the native Messages request into Bedrock's InvokeModel format and requires `model` and `small_fast_model`. `"mantle"` forwards the native Messages request body unchanged with AWS SigV4 signing and passes through the client's model; requires `region` (the Mantle base URL is not a canonical Bedrock URL, so the region is never derived from `base_url`). Requires Coder v2.36.0 or later for the Mantle protocol. Omit (or set to `"invoke-model"`) to use the default. Removing the attribute clears it back to the default; Coder maps an empty value to `"invoke-model"`.
                            type: string
                          region:
                            description: |-
                              (String) AWS region for Bedrock. If omitted, derived from the canonical Bedrock base_url attribute.
                              AWS region for Bedrock. If omitted, derived from the canonical Bedrock `base_url` attribute.
                            type: string
                          roleArn:
                            description: |-
                              (String) ARN of an AWS IAM role to assume via STS before calling Bedrock. The base identity (the AWS SDK default credential chain or the static credentials) signs the AssumeRole call, and the temporary credentials sign Bedrock requests. Omit to call Bedrock with the base identity directly. Requires Coder v2.35.0 or later.
                              ARN of an AWS IAM role to assume via STS before calling Bedrock. The base identity (the AWS SDK default credential chain or the static credentials) signs the AssumeRole call, and the temporary credentials sign Bedrock requests. Omit to call Bedrock with the base identity directly. Requires Coder v2.35.0 or later.
                            type: string
                          smallFastModel:
                            description: |-
                              (String) Small/fast Bedrock model identifier used for background tasks.
                              Small/fast Bedrock model identifier used for background tasks.
                            type: string
                        type: object
                    type: object
                  type:
                    description: |-
                      compat, openrouter, vercel, and copilot.
                      AI provider type. Valid values are `openai`, `anthropic`, `azure`, `bedrock`, `google`, `openai-compat`, `openrouter`, `vercel`, and `copilot`.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.baseUrl is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.baseUrl)
                || (has(self.initProvider) && has(self.initProvider.baseUrl))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
          status:
            description: AIProviderStatus defines the observed state of AIProvider.
            properties:
              atProvider:
                properties:
                  apiKeyMasked:
                    description: |-
                      (String) Masked API key value returned by Coder for display only.
                      Masked API key value returned by Coder for display only.
                    type: string
                  apiKeyWoVersion:
                    description: |-
                      only API key. Required when api_key_wo is set; bump it whenever api_key_wo changes to rotate the stored key.
                      Version for the write-only API key. Required when `api_key_wo` is set; bump it whenever `api_key_wo` changes to rotate the stored key.
                    type: number
                  baseUrl:
                    description: |-
                      (String) Absolute HTTP(S) base URL for the upstream provider endpoint.
                      Absolute HTTP(S) base URL for the upstream provider endpoint.
                    type: string
                  createdAt:
                    description: |-
                      (Number) Creation timestamp as Unix seconds.
                      Creation timestamp as Unix seconds.
                    type: number
                  displayName:
                    description: |-
                      (String) Display name shown in Coder. If omitted, defaults to the provider name.
                      Display name shown in Coder. If omitted, defaults to the provider name.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Whether this AI provider is enabled. Defaults to true.
                      Whether this AI provider is enabled. Defaults to true.
                    type: boolean
                  id:
                    description: (String) AI provider ID.
                    type: string
                  name:
                    description: |-
                      (String) Unique provider name. Must be lowercase alphanumeric words separated by hyphens.
                      Unique provider name. Must be lowercase alphanumeric words separated by hyphens.
                    type: string
                  settings:
                    description: specific provider settings. (see below for nested
                      schema)
                    properties:
                      bedrock:
                        description: (Attributes) AWS Bedrock settings. Valid only
                          for type = "bedrock" or type = "anthropic". (see below for
                          nested schema)
                        properties:
                          credentialsWoVersion:
                            description: |-
                              only credentials. Bump this value to send, rotate, or clear credentials.
                              Version for Bedrock write-only credentials. Bump this value to send, rotate, or clear credentials.
                            type: number
                          externalId:
                            description: |-
                              (String) STS external ID the server generates and sends on the AssumeRole call when role_arn is set. Reference it in the assumed role's trust policy sts:ExternalId condition. Null until role_arn is first configured; stable afterwards. Requires Coder v2.36.0 or later.
                              STS external ID the server generates and sends on the AssumeRole call when `role_arn` is set. Reference it in the assumed role's trust policy `sts:ExternalId` condition. Null until `role_arn` is first configured; stable afterwards. Requires Coder v2.36.0 or later.
                            type: string
                          model:
                            description: |-
                              (String) Primary Bedrock model identifier.
                              Primary Bedrock model identifier.
                            type: string
                          protocol:
                            description: |-
                              model" (default) translates the native Messages request into Bedrock's InvokeModel format and requires model and small_fast_model. "mantle" forwards the native Messages request body unchanged with AWS SigV4 signing and passes through the client's model; requires region (the Mantle base URL is not a canonical Bedrock URL, so the region is never derived from base_url). Requires Coder v2.36.0 or later for the Mantle protocol. Omit (or set to "invoke-model") to use the default. Removing the attribute clears it back to the default; Coder maps an empty value to "invoke-model".
                              Bedrock wire protocol. `"invoke-model"` (default) translates the native Messages request into Bedrock's InvokeModel format and requires `model` and `small_fast_model`. `"mantle"` forwards the native Messages request body unchanged with AWS SigV4 signing and passes through the client's model; requires `region` (the Mantle base URL is not a canonical Bedrock URL, so the region is never derived from `base_url`). Requires Coder v2.36.0 or later for the Mantle protocol. Omit (or set to `"invoke-model"`) to use the default. Removing the attribute clears it back to the default; Coder maps an empty value to `"invoke-model"`.
                            type: string
                          region:
                            description: |-
                              (String) AWS region for Bedrock. If omitted, derived from the canonical Bedrock base_url attribute.
                              AWS region for Bedrock. If omitted, derived from the canonical Bedrock `base_url` attribute.
                            type: string
                          roleArn:
                            description: |-
                              (String) ARN of an AWS IAM role to assume via STS before calling Bedrock. The base identity (the AWS SDK default credential chain or the static credentials) signs the AssumeRole call, and the temporary credentials sign Bedrock requests. Omit to call Bedrock with the base identity directly. Requires Coder v2.35.0 or later.
                              ARN of an AWS IAM role to assume via STS before calling Bedrock. The base identity (the AWS SDK default credential chain or the static credentials) signs the AssumeRole call, and the temporary credentials sign Bedrock requests. Omit to call Bedrock with the base identity directly. Requires Coder v2.35.0 or later.
                            type: string
                          smallFastModel:
                            description: |-
                              (String) Small/fast Bedrock model identifier used for background tasks.
                              Small/fast Bedrock model identifier used for background tasks.
                            type: string
                        type: object
                    type: object
                  type:
                    description: |-
                      compat, openrouter, vercel, and copilot.
                      AI provider type. Valid values are `openai`, `anthropic`, `azure`, `bedrock`, `google`, `openai-compat`, `openrouter`, `vercel`, and `copilot`.
                    type: string
                  updatedAt:
                    description: |-
                      (Number) Last update timestamp as Unix seconds.
                      Last update timestamp as Unix seconds.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
